---
paths:
  - 'app/Auth/**'
---

# Auth

## Custom external session auth guard
Register a custom Laravel guard in the service provider and return a session-backed user object that implements the auth contract for external authentication.
